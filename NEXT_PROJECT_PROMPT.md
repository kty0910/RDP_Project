# 다음 프로젝트용 Codex 개발 프롬프트

이 문서는 C# WinForms 기반 업무용 Windows 프로그램을 만들 때, 불필요한 시행착오를 줄이기 위한 기준 프롬프트입니다.  
새 프로젝트를 시작할 때 이 파일을 Codex에게 전달하고, “이 MD 파일 기준으로 만들어줘”라고 요청하면 됩니다.

## 기본 요청 문장

```text
이 MD 파일 내용을 기준으로 C# WinForms 프로그램을 만들어줘.
처음부터 완성형 구조를 잡고, 단계별로 빌드 가능한 상태를 유지하면서 구현해줘.
UI는 사용자가 실제 업무에서 반복적으로 사용할 수 있게 여백을 줄이고, 정보가 한눈에 보이도록 만들어줘.
```

## 개발 환경

- 언어: C#
- 프레임워크: .NET 8 이상
- UI: WinForms
- 대상 OS: Windows
- IDE 기준: Visual Studio 2022 / VS Code
- 프로젝트 기본 경로: `E:\Codex`
- 솔루션 구조는 기능별 프로젝트로 분리한다.

## 프로젝트 구조 원칙

```text
Solution
├─ Product.Client
├─ Product.Server
├─ Product.Server.Service
├─ Product.Bridge 또는 중개 기능
├─ installer
├─ publish
└─ installer-output
```

공통 폴더 구조:

```text
Forms
Models
Services
Networking
Config
Logging
Utilities
```

## 구현 원칙

- 한 번에 크게 만들기보다, 기능 단위로 구현하고 매번 빌드한다.
- 기존 코드 스타일을 유지하고, 불필요한 추상화는 만들지 않는다.
- UI 문자열은 처음부터 정상 한글 UTF-8로 작성한다.
- 사용자에게 보이는 용어는 개발자 용어보다 업무 용어를 우선한다.
- 예: `Bridge` 대신 `중개 PC`, `Target`은 필요할 때만 그대로 사용한다.
- 기능 구현 후에는 Debug 빌드뿐 아니라 설치파일에 들어갈 Release publish까지 확인한다.

## UI 설계 기준

- 업무용 도구는 랜딩 페이지처럼 만들지 않는다.
- 첫 화면에서 실제 기능을 바로 사용할 수 있어야 한다.
- 전체 여백은 작게 유지한다. 기본 여백은 8~10px 수준을 권장한다.
- 버튼, 셀, 박스가 불필요하게 큰 공간을 차지하지 않게 한다.
- 텍스트는 가로/세로 가운데 정렬이 필요한 곳에서는 실제 시각적 중앙에 오게 한다.
- DataGridView 헤더는 정렬 아이콘 때문에 텍스트가 밀리지 않도록 `SortMode = NotSortable`을 검토한다.
- 행 높이, 헤더 높이는 사용자가 위아래로 조절하지 못하게 막는다.
- 열 너비는 필요하면 사용자가 좌우로 조절할 수 있게 한다.
- 버튼 텍스트가 잘리지 않도록 높이와 폭을 여유 있게 잡는다.
- placeholder는 텍스트 입력 전까지 유지되어야 한다.
- placeholder는 포커스/마우스 hover에도 깜빡이지 않게 구현한다.

## Client UI 기준

상단 영역:

- 내 PC 정보
- 원격 목록 백업
- 원격 PC 추가

내 PC 정보:

- PC 이름
- IP 주소
- 사용자 계정명은 꼭 필요한 경우가 아니면 표시하지 않는다.

원격 목록 백업:

- 내보내기
- 가져오기
- 버튼 텍스트가 잘리지 않게 높이를 확보한다.

원격 PC 추가:

- 상단 박스에는 입력칸을 직접 노출하지 말고 `원격 PC 추가` 버튼만 둔다.
- 버튼 클릭 시 원격 PC 정보 입력/수정 창을 띄운다.

원격 PC 목록:

- 기본 컬럼:
  - 빈 컬럼: `+` / `-` 버튼
  - 원격 PC 이름
  - 연결 상태
  - 접속 인원
  - 상태 체크
  - 원격 접속
- `+` 클릭 시 추가 정보를 작은 팝업으로 표시한다.
- 추가 정보:
  - 원격 PC IP
  - 접속자 PC
  - 접속자 IP
- 추가 정보가 열리면 `+`를 `-`로 바꾼다.
- `-`를 클릭하면 추가 정보 팝업을 닫고 다시 `+`로 바꾼다.
- 그리드 아래로 행이 늘어나는 방식보다 팝업 방식이 좋다.
- 선택, 포커스, 클릭 때문에 커서나 선택 셀이 튀지 않도록 한다.

전체 상태 체크:

- 원격 PC 목록 라벨 오른쪽에 `전체 상태 체크` 버튼을 둔다.
- 클릭 즉시 모든 행이 `확인 중` / `종료` 상태로 바뀌어야 한다.
- 각 PC의 확인 결과가 도착할 때마다 그리드를 즉시 갱신한다.
- 내부 상태만 바뀌고 UI가 늦게 바뀌는 구조는 피한다.

개별 상태 체크:

- 기본 상태는 `시작`
- 클릭 시 상태 체크를 시작하고 버튼은 `종료`로 바뀐다.
- 다시 클릭하면 즉시 체크를 중단하고 `시작`으로 돌아간다.
- 체크 중인 요청이 완료되더라도 이미 종료된 상태면 UI를 다시 체크 중으로 되돌리지 않는다.
- 체크 주기는 1초를 기본으로 하되, 필요 시 설정값으로 분리한다.
- 상태 체크는 1분 후 자동 종료되게 한다.

원격 접속:

- 상태 체크를 시작하지 않은 PC는 원격 접속 버튼을 비활성화한다.
- 비활성화 상태에서도 버튼 텍스트는 `접속`으로 유지하고, 글씨만 연한 회색으로 표시한다.
- 연결 상태 또는 접속자 상태가 확인 불가이면 바로 접속하지 말고 경고를 띄운다.
- 사용자가 그래도 접속하겠다고 선택하면 접속을 진행한다.
- 접속자가 있으면 경고 후 사용자가 확인한 경우에만 접속한다.

## 원격 PC 정보 입력/수정 창

필드 순서:

1. PC 이름
2. IP
3. ID
4. PW
5. 중개 PC 경유 체크박스
6. 중개 PC IP
7. 중개 PC Port
8. Target 이름
9. Token

placeholder:

- PC 이름: `ex. V5`
- IP: `ex. 127.0.0.1`
- ID: `원격 PC ID`
- PW: `원격 PC PW`
- 중개 PC IP: `중개 PC IP`
- Target 이름: `Target 이름`
- Token: `Token`

버튼:

- 완료
- 취소
- 삭제

삭제 버튼은 수정 창에서만 표시한다.  
추가 창에서는 삭제 버튼을 숨긴다.

검증:

- PC 이름, IP, ID, PW는 필수 입력값이다.
- 중개 PC 경유를 선택했다면 중개 PC IP, Target 이름, Token도 필수 입력값이다.
- 입력 시 네트워크 검증을 매번 수행하면 느릴 수 있으므로, 기본 추가/수정은 빠르게 저장하고 상태 체크에서 연결 여부를 판단한다.

## 데이터 저장

- 원격 PC 목록은 코드에 하드코딩하지 않는다.
- 별도 파일에 저장한다.
- 비밀번호는 평문 저장하지 않는다.
- Windows 사용자 기준 암호화 또는 AES 기반 암호화를 사용한다.
- 내보내기/가져오기 기능을 제공한다.
- 가져오기/내보내기는 사용자가 직접 복호화를 부탁하지 않아도 프로그램 안에서 처리되어야 한다.

## Server 기준

목표:

- 원격 PC에 설치되어 현재 RDP 접속 상태를 제공한다.
- Client가 HTTP API로 상태를 조회할 수 있게 한다.
- 재부팅 후 자동 실행을 지원한다.

세션 조회:

- 가능하면 WTS API 단독 사용을 우선한다.
- `quser/qwinsta`는 보조 또는 초기 구현용으로만 고려한다.
- 1초 주기 요청이 여러 Client에서 들어올 수 있으므로 Server 내부 캐싱을 둔다.
- Client가 100대 이상 동시에 조회해도 매 요청마다 무거운 명령을 실행하지 않게 한다.

Server UI:

- 현재 RDP 접속자 목록 표시
- 접속 PC 이름 표시
- 접속 PC IP 표시
- 접속 시작 시간
- 접속 종료 시간
- Session ID는 UI에 표시하지 않고 로그에만 남긴다.
- 로그 테이블은 빈 가로 공간이 생기지 않도록 열 너비를 맞춘다.

로그:

- 기본 경로: 설치 경로 아래 `Logs`
- 파일명 예: `Server_20260527.txt`
- 너무 자세한 stack trace를 기본 로그에 남기지 않는다.
- 일반 상태 조회 로그는 남기지 않는다.
- 에러는 한 줄 요약 위주로 남긴다.

Tray:

- X 버튼 클릭 시 완전 종료가 아니라 트레이로 숨긴다.
- 실제 종료는 트레이 메뉴의 종료에서만 수행한다.
- 트레이 메뉴:
  - Open
  - Refresh Status
  - Start with Windows 또는 자동 실행 제어
  - Exit

서비스:

- 여러 사용자 계정이 있는 PC에서는 시작프로그램보다 Windows Service 방식이 더 적합하다.
- UI 프로그램과 백그라운드 서비스는 분리한다.
- 재부팅 시 자동 실행을 선택한 경우 백그라운드 서비스가 실행되어야 하며, UI가 자동으로 뜨면 안 된다.

## 중개 PC 기능

용어:

- 사용자 UI에서는 `Bridge` 대신 `중개 PC`라고 표현한다.
- 내부 코드명은 기존 구조상 Bridge를 써도 되지만, 화면에는 중개 PC로 표시한다.

목표:

- 내 PC → 중개 PC → 원격 PC 구조를 지원한다.
- Client는 중개 PC를 통해 원격 PC 상태 체크와 RDP 접속을 수행할 수 있어야 한다.
- 원격 PC에도 Server가 설치되어 있으면 중개 PC를 통해 상태 체크 가능해야 한다.

권장 구조:

- 별도 콘솔 Bridge 프로그램보다 Server에 중개 기능을 통합하는 구조를 우선 고려한다.
- Server UI에는 중개 기능 상태만 표시한다.
- 설정 편집은 JSON 파일에서 처리해도 된다.
- UI에서는 ON/OFF 정도만 제공한다.

중개 설정 예:

```json
{
  "enabled": true,
  "rdpPortStart": 13389,
  "rdpPortEnd": 13489,
  "token": "CHANGE_ME",
  "allowedTargets": [
    {
      "name": "V5",
      "host": "192.168.250.3",
      "apiPort": 5000,
      "rdpPort": 3389
    }
  ]
}
```

주의:

- 실행 중인 Server가 설정 파일을 종료 시점에 덮어쓰면 안 된다.
- 실행 중 설정 파일을 수정한 경우, 종료하면서 이전 설정으로 원복되는 문제가 생기지 않게 한다.
- 설정 저장은 명시적으로 변경한 경우에만 수행한다.

## 통신

- 기본 통신 방식: HTTP + JSON
- 기본 API Port: 5000
- RDP 기본 Port: 3389
- 중개 RDP 포트 범위 예: 13389~13489
- API 실패 시 사용자가 이해할 수 있는 한글 메시지를 띄운다.
- `An error occurred while sending the request.` 같은 원문 메시지만 보여주지 않는다.

## Installer 기준

설치 도구:

- Inno Setup 6

설치 대상:

- Client
- Server
- Server Service
- 중개 기능이 별도 프로그램이면 Bridge

설치 경로 기본값:

- `C:\RDP\Client`
- `C:\RDP\Server`
- `C:\RDP\Bridge`

설치 창:

- 설치 / 삭제 라디오 선택 페이지를 먼저 표시한다.
- 설치 선택 시 설치할 구성요소를 고른다.
- 기본 체크는 모두 해제한다.
- 선택한 구성요소의 경로만 표시한다.
- Client만 선택하면 Client 경로만 표시한다.
- Server만 선택하면 Server 경로만 표시한다.
- Client + Server를 선택하면 두 경로를 모두 표시한다.

추가 작업:

```text
=== Client 추가 작업 ===
바탕화면 아이콘 생성
시작 메뉴 바로가기 등록
Windows 방화벽 예외 등록

=== Server 추가 작업 ===
바탕화면 아이콘 생성
시작 메뉴 바로가기 등록
Windows 방화벽 예외 등록
Windows 부팅 시 자동 실행
```

설치 후 실행:

- 설치가 완료된 후 마지막 페이지에서 선택하게 한다.
- 설치 전 추가 작업 페이지에 섞지 않는다.

실행 중 프로세스 감지:

- 설치 또는 삭제 전에 실행 중인 Client/Server/Service를 감지한다.
- 실행 중이면 안내한다.
- 버튼:
  - 설치 취소
  - 다시 시도
- 다시 시도 시 프로세스를 다시 감지한다.

삭제:

- 삭제 진행 상황을 로딩바로 보여준다.
- 완료 후 사용자가 누를 수 있는 완료/종료 버튼을 활성화한다.
- 방화벽 예외, 서비스 등록, 자동 실행 등록을 제거한다.
- 설치된 Client/Server 폴더뿐 아니라 비어 있는 상위 `C:\RDP` 폴더도 정리한다.
- 단, 사용자가 직접 만든 다른 중요한 파일이 있는 경우 삭제 정책을 신중하게 정한다.

버전:

- 형식: `v큰패치.개선패치.버그패치`
- 최초 개발 단계: `v1.0.0`
- 설치파일명 예: `RemoteMonitor_Setup_v1.0.0.exe`

중요:

- `dotnet build`만으로는 설치파일에 최신 변경이 반영되지 않는다.
- 설치파일을 만들기 전 반드시 `dotnet publish`를 다시 수행해야 한다.
- Inno Setup은 `publish` 폴더의 결과물을 압축하므로, publish가 실패했는데 installer만 만들어지면 오래된 exe가 들어갈 수 있다.
- 설치파일 생성 스크립트는 publish 실패 시 즉시 중단되게 한다.

권장 빌드 흐름:

```powershell
dotnet build RemoteSessionMonitor.sln
powershell.exe -ExecutionPolicy Bypass -File installer\build-installer.ps1
```

## 테스트 체크리스트

Client:

- 프로그램 시작 시 특정 TextBox에 포커스가 자동으로 가지 않는다.
- 원격 PC 추가/수정/삭제가 정상 동작한다.
- placeholder가 입력 전까지 유지된다.
- placeholder가 마우스 hover/focus에서 깜빡이지 않는다.
- 전체 상태 체크가 즉시 UI에 반영된다.
- 개별 상태 체크 시작/종료가 즉시 반영된다.
- 상태 체크 중 종료를 누르면 요청이 취소되고 UI가 다시 돌아오지 않는다.
- 상태 체크 미실행 상태에서는 원격 접속이 비활성화된다.
- 접속자 있음/확인 불가 상태에서 원격 접속 경고가 뜬다.
- 원격 PC 목록 내보내기/가져오기가 정상 동작한다.
- 트레이 메뉴에서 전체 상태 체크와 원격 접속이 동작한다.

Server:

- RDP 접속 시 접속 PC 이름/IP/시작 시간이 표시된다.
- RDP 해제 시 종료 시간이 같은 로그 행에 표시된다.
- 로그 파일명이 `Server_yyyyMMdd.txt` 형식이다.
- Session ID가 UI에는 보이지 않고 로그에만 남는다.
- X 버튼 클릭 시 트레이로 숨겨진다.
- 서비스 자동 실행 시 UI가 자동으로 뜨지 않는다.
- 로그가 너무 장황하지 않다.

중개 PC:

- 중개 PC를 통해 원격 PC 상태 체크가 가능하다.
- 중개 PC를 통해 RDP 접속이 가능하다.
- Target 이름과 Token 오류 시 사용자가 이해할 수 있는 메시지가 나온다.
- 설정 파일 수정 후 종료해도 설정이 이전 내용으로 원복되지 않는다.

Installer:

- Client만 설치 가능하다.
- Server만 설치 가능하다.
- Client + Server 동시 설치 가능하다.
- 선택한 구성요소의 경로만 보인다.
- 추가 작업이 Client/Server별로 분리되어 있다.
- 방화벽 예외가 등록/삭제된다.
- 서비스가 등록/삭제된다.
- 삭제 진행률이 보인다.
- 설치파일에 최신 publish 결과물이 들어간다.

