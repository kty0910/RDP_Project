# Remote Monitor Release Notes

프로그램 변경 이력을 날짜별로 기록한다.

- 현재 버전: `v1.1.2`
- 최신 변경일: `2026-07-29`
- 날짜가 명확하지 않은 연속 작업은 기간으로 묶어 기록한다.
- 비밀번호, 계정, Token 등 민감정보는 기록하지 않는다.

---

## 2026-07-29

### v1.1.2 패치

- 내부망에서 주로 사용하는 무토큰 구성을 기본판으로 변경
- 기본판은 `EnableBridgeToken=false`, `publish`, `installer-output`, `RemoteMonitor_Setup_v1.1.2.exe` 명칭 사용
- 토큰판은 `EnableBridgeToken=true`, `publish-token`, `installer-output-token`, `RemoteMonitor_Setup_v1.1.2_Token.exe` 명칭 사용
- 설치 빌드 스크립트에 `-Token` 선택 항목을 추가하고 README의 빌드·배포 명칭을 새 기준으로 정리

### v1.1.1 패치

- Client와 Server 버전을 `1.1.1`로 갱신
- 일반판과 무토큰판 설치 파일을 `RemoteMonitor_Setup_v1.1.1.exe`로 갱신
- Client 원격 PC 목록에서 `연결 불가`, `확인 불가` 상태를 옅은 빨간색 행으로 표시
- 접속자가 있는 원격 PC를 옅은 노란색 행으로 표시하여 경고 상태를 쉽게 구분하도록 개선
- 상태 체크 대기·확인 중과 정상 상태는 기존 흰색 행을 유지하고, 선택된 행과 버튼 영역에도 상태 색상을 일관되게 적용
- 프로그램 시작 및 Windows 로그인 후 자동 실행 시 전체 상태 확인 시작 대기 시간을 5초에서 1초로 단축
- 전체 상태 확인 도중 원격 PC를 추가·수정·삭제하면 목록 열거 예외가 발생할 수 있던 문제 수정

### v1.1.0 배포

- Client와 Server 버전을 `1.1.0`으로 갱신
- 일반판과 무토큰판 설치 파일을 `RemoteMonitor_Setup_v1.1.0.exe`로 갱신

### Client 원격 PC 부가 설명

- 원격 PC 추가·수정 창에 한 줄 요약을 입력하는 `부가 설명` 항목 추가
- `상세` 버튼을 통해 요약과 긴 설명을 함께 편집하는 별도 창 추가
- Client 메인 원격 PC 목록에 부가 설명 요약 열 추가
- Client 메인 창과 부가 설명 열의 기본 폭을 확대하여 긴 요약의 가독성 개선
- 기존 원격 PC 목록과 백업 파일은 설명이 없는 빈 값으로 호환되며, 신규 설명은 암호화 저장 및 백업·복원 대상에 포함

### Server 원격 PC 부가 설명

- Server 원격 PC 추가·수정 창에 `부가 설명` 요약 입력란과 `상세` 버튼 추가
- Server 원격 PC 목록에 부가 설명 요약 열 추가
- 요약과 긴 상세 설명을 `bridge_settings.json`에 저장하고 기존 설정 파일은 빈 설명으로 호환
- Server 원격 PC 목록 창의 폭을 확대하여 이름, 설명, IP 및 Port 열의 가독성 개선

### Client 접속자 상세창

- 접속자 상세 정보창을 닫은 뒤 `-` 버튼이 다음 상태 갱신까지 남아 있던 문제 수정
- 상세 정보창을 닫는 즉시 해당 버튼이 `+`로 복원되도록 개선

### Client 원격 PC 목록 순서

- 원격 PC를 새로 추가할 때 기존 항목 앞에 표시될 수 있던 문제 수정
- 신규 원격 PC가 저장된 등록 순서에 따라 항상 목록 마지막 행에 표시되도록 개선

---

## 2026-07-24

### 자동 시작

- Client 자동 시작 옵션 추가: 수동 실행만 가능 → 설치 시 `Windows 로그인 시 자동 실행` 선택 가능
- Client 실행 후 상태 확인 방식 변경: 사용자가 직접 전체 체크 시작 → 실행 5초 후 전체 체크 자동 시작
- Client 트레이 메뉴 개선: 상세 모드 열기 중심 → `전체 체크 시작`, `전체 체크 종료` 추가
- 재부팅 시 Client 자동 시작 방식 변경: 로그인 시 화면에 상세 UI 표시 → 트레이에서 조용히 시작
- 재부팅 시 Server UI 자동 시작 방식 변경: 로그인 시 화면에 Server UI 표시 → 트레이에서 조용히 시작
- 수동 실행 방식 유지: exe 직접 실행 또는 설치 완료 후 실행 → 기존처럼 상세 UI 표시

### Server 원격 PC 목록

- Server 원격 PC 관리 방식 변경: `bridge_settings.json` 직접 편집 → Server UI의 `원격 PC 목록`에서 관리
- 원격 PC 식별 방식 개선: IP·Port 조합으로만 식별 → 사용자가 지정한 PC 이름 표시
- 원격 PC 등록 기능 추가: Server UI에서 PC 이름, IP, Status Port, RDP Port 입력 가능
- 원격 PC 수정·삭제 기능 추가: 목록 항목 더블클릭 → 수정/삭제 창 표시
- 원격 PC 순서 변경 기능 추가: 고정된 등록 순서 → `목록 편집`에서 `▲`, `▼`로 재배치
- 순서 저장 방식 추가: 목록 편집 `확인` → 화면과 `bridge_settings.json`의 순서 동시 저장
- 순서 취소 방식 추가: 목록 편집 `취소` → 변경 내용 폐기
- Server 트레이 동작 개선: 트레이 아이콘 더블클릭 무응답 → Server UI 표시

### 중개 및 인증

- 중개 대상 설정 방식 단순화: Client에서 Target과 Token을 모두 직접 관리 → Server에 등록한 원격 PC 목록 중심으로 관리
- 내부망용 무토큰 배포본 추가: Token 필수 단일 배포 → 일반판과 무토큰판 병행 배포
- 무토큰판 출력 경로 추가: `installer-output-notoken`
- 중개 접속 계정 처리 개선: 같은 중개 주소에 저장된 다른 계정이 사용됨 → 선택한 원격 PC의 ID에 맞춰 자격 증명 교체
- 자격 증명 유지 방식 개선: 실행 세션 동안만 사용 → 사용자가 Windows의 `기억`을 선택하면 재부팅 후에도 사용 가능

### 설치 프로그램

- 구성요소 기본 선택 변경: Client와 Server 모두 기본 체크 → 둘 다 기본 체크 해제
- 설치 선택 방식 개선: 사용하지 않을 항목을 해제 → 사용할 Client/Server를 사용자가 직접 선택
- 구성요소 선택 유지 기능 추가: 이전 화면 이동 후 선택 초기화 → 사용자가 선택한 상태 유지
- 자동 시작 레지스트리 실행 인수 추가: exe만 실행 → `--tray` 인수와 함께 실행
- 설치 전 Client 처리 개선: 사용자가 직접 Client 종료 후 재시도 → 설치 프로그램이 종료 여부 확인 후 처리
- 설치 전 Server 처리 개선: Server UI만 검사 → Server UI와 Server Service를 함께 검사하고 종료
- 구성요소별 실행 검사 개선: Client만 설치해도 Server 종료 요구 → 선택한 구성요소만 검사
- 신규 Client 초기 데이터 수정: 개발 PC의 원격 목록 포함 가능 → 빈 원격 PC 목록으로 설치
- 신규 Server 초기 데이터 수정: 개발 PC의 Bridge 설정 포함 가능 → 빈 원격 PC 목록으로 설치
- 설치 배포일 갱신: `2026-07-24`

---

## 2026-07-20 ~ 2026-07-23

### 설치 및 삭제

- 삭제 대상 선택 기능 추가: Client와 Server 일괄 삭제 → Client/Server 개별 선택 삭제
- 삭제 경로 오류 수정: `C:\RDP\Client\RDP\Server`와 같은 중첩 경로 생성 → 선택한 실제 설치 경로만 삭제
- 삭제 완료 화면 수정: 종료 버튼 중복 표시 → 종료 버튼 하나만 표시
- 삭제 완료 동작 수정: 종료 클릭 시 `설치가 완료되지 않았습니다` 경고 → 정상 종료
- 삭제 후 실행 항목 수정: 삭제 화면에 Client/Server 실행 체크박스 표시 → 삭제 모드에서는 숨김
- Server 삭제 개선: Server UI 파일만 삭제 → Service, 자동 시작 항목, 바로가기와 설치 파일 함께 정리
- 재설치 실행 검사 개선: 트레이 Server UI 종료 후에도 설치 불가 → Server Service까지 자동 종료 후 설치
- 잔여 설치 감지 개선: 실제 Client 폴더가 없어도 설치된 것으로 판단 → 설치 경로와 설치 정보 정합성 기준으로 판단

### RDP 연결

- 최근 연결 표시 방식 변경: `RemoteMonitor_날짜`로 표시 → 대상 IP를 식별할 수 있도록 개선
- RDP ID 전달 수정: Windows에 남아 있던 다른 계정 사용 → 원격 PC 목록에 등록한 ID 우선 사용
- 비밀번호 저장 정책 변경: 프로그램이 임의로 자격 증명 영구 저장 → Windows 자격 증명 창에서 사용자가 `기억`을 선택한 경우에만 저장

---

## 2026-07-14

### Server Status Port

- Status Port 적용 방식 변경: 변경 후 사용자가 수동으로 Service 재시작 → 확인 시 자동 재시작
- Status Port 취소 동작 변경: 변경값 저장 후 수동 적용 필요 → 변경 취소 후 기존 Port 유지
- Server 상태 영역 배치 수정: `Last checked` 하단 잘림 → Status Port, Bridge, Target 영역을 아래로 이동
- Server 상태 영역 여백 재조정: 항목 이동 후 과도한 여백 발생 → 텍스트가 보이는 최소 간격으로 축소

### 상태 확인 안정화

- 일시적인 통신 실패 처리 변경: HTTP 요청 1회 실패 시 즉시 `확인 불가` → 3회 연속 실패 시 `확인 불가`
- 통신 복구 처리 유지: 다음 HTTP 요청 성공 시 정상 상태로 즉시 복구

---

## 2026-07-08

### Status Port 사용자 설정

- Server Status Port 설정 기능 추가: 5000 고정 → Server UI에서 직접 변경 가능
- Client 원격 PC 정보 확장: IP만 입력 → IP와 Status Port를 별도로 입력
- 기본 Status Port 설정: `5000`
- Client 안내 문구 추가: `Server의 Status Port 입력`
- 중개 PC Port 안내 문구 추가: `중개 PC의 Port 입력`
- 방화벽 처리 정책 변경: 설치 프로그램이 자동으로 방화벽 예외 등록 → 자동 등록 기능 제거
- 보안 정책 변경: 프로그램이 방화벽을 임의 변경 → 사용자가 사내 보안 정책에 따라 직접 설정

---

## 2026-07-01 ~ 2026-07-02

### 외부 IP와 Port

- RDP 주소 입력 확장: IP만 입력 → `IP:RDP Port` 형식 지원
- RDP Port 처리 추가: 주소 뒤 Port가 있으면 기본 3389 대신 입력한 Port 사용
- 상태 조회 구조 구분: RDP 외부 Port를 Status 조회에도 사용 → RDP Port와 Status Port를 별도 처리
- 외부 상태 조회 문제 분석: RDP 접속 가능하지만 접속 인원 오표시 → Status API가 다른 내부 PC로 포워딩되는 문제 확인
- 다중 내부 PC 상태 조회 방식 정리: 공인 IP 하나로 일괄 조회 → PC별 Status Port 포워딩 또는 중개 PC 사용

---

## 2026-06-08 ~ 2026-06-10

### Server Service와 UI

- Server 실행 충돌 수정: Service와 Server UI가 같은 Port를 동시에 열어 소켓 오류 → Service 실행 중에는 UI 모드로 전환
- Server 역할 분리: 자동 실행 여부와 관계없이 UI가 API Server 역할 수행 → Service 설치 시 Service가 API 담당, UI는 상태 표시
- 독립 실행 방식 유지: Service가 없으면 Server UI가 직접 API Server 역할 수행
- Server 한글 표시 수정: 열 제목과 상태 문구 깨짐 → UTF-8 한글 정상 표시
- Server 트레이 시작 메뉴 정리: 시작 프로그램과 Service 역할 혼재 → Service와 UI 자동 시작 역할 분리

### Client 실행

- Client 중복 실행 방지 추가: 여러 Client 프로세스 실행 가능 → 한 번에 하나만 실행
- 상태 확인 중 UI 흔들림 수정: 1초 갱신마다 셀 위치 변경 → 기존 셀 값만 갱신
- 네트워크 변경 반영 개선: Client 재실행 후 IP 변경 → 네트워크 변경 이벤트와 주기 갱신으로 자동 반영

### 설치 및 배포

- 설치 완료 실행 항목 추가: 설치 후 수동 실행 → `Client 실행`, `Server UI 실행` 선택 가능
- 배포 방식 변경: self-contained 대용량 배포 → framework-dependent 배포
- 설치 파일 용량 축소: .NET Runtime 포함 → Runtime 별도 배포
- 필수 Runtime 지정: `.NET Desktop Runtime 8 x64`

### 사용자 가이드

- RDP 프로그램 설치·사용 가이드 제작
- 기존 사내 PowerPoint 양식 적용
- 실제 설치 프로그램과 Client/Server 화면 캡처 기반 설명 추가
- PowerPoint 복구 경고가 발생하지 않도록 문서 구조 수정

---

## 2026-05-27

### Client UI 최적화

- 원격 PC 추가 방식 변경: 메인 화면에서 직접 입력 → `원격 PC 추가` 버튼과 별도 입력 창
- 원격 PC 목록 정보 축소: 모든 정보를 열로 표시 → 핵심 정보만 표시
- 목록 기본 열 정리: 원격 PC 이름, 연결 상태, 접속 인원, 상태 체크, 원격 접속
- 상세 정보 표시 추가: `+` 클릭 → 원격 PC IP, 접속 PC 이름, 접속 PC IP 팝업
- 상세 정보 닫기 추가: 팝업 표시 중 `+` → `-`, 다시 클릭 시 닫기
- 원격 PC 목록 순서 편집 추가: 등록 순서 고정 → 별도 목록 편집 창에서 이동
- 그리드 정렬 변경: 텍스트 위치 불균형 → 헤더와 셀 모두 가로·세로 가운데 정렬
- 불필요한 여백 축소: 큰 박스와 넓은 셀 → 약 10px 기준의 조밀한 UI
- 상단 박스 너비 재조정: 개별 박스 크기 불일치 → 원격 PC 목록 전체 너비에 맞춤

### Client 입력 창

- 입력 항목 명칭 변경: `RDP ID`, `RDP PW` → `ID`, `PW`
- 중개 용어 변경: `Bridge` → `중개 PC`
- 중개 선택 문구 수정: `중개 PC 경우` → `중개 PC 경유`
- Placeholder 동작 수정: 클릭 시 사라짐 → 실제 텍스트 입력 전까지 유지
- Placeholder 색상 수정: 검정·회색 깜빡임 → 회색 고정

### 상태 확인 UI

- 전체 상태 확인 버튼 추가
- 전체 상태 확인 시작/종료 기능 분리
- 버튼 크기와 위치를 원격 PC 목록 오른쪽 테두리에 맞게 정렬
- 목록 갱신 시 선택 셀이 첫 번째 열로 이동하는 문제 수정
- 상태 확인 버튼과 원격 접속 버튼 셀 더블클릭 시 수정 창이 열리는 문제 수정
- 상태 확인 중지 시 진행 중 요청이 완료된 후 다시 상태가 바뀌는 문제 수정

---

## 2026-05-21 ~ 2026-05-22

### 세션 확인 방식

- 세션 수집 방식 변경: `quser`·`qwinsta` 명령 실행 → Windows WTS API 단독 사용
- 언어 의존성 제거: 명령 출력 문자열 분석 → Windows API 구조체 사용
- Server 상태 처리 변경: Client 요청마다 세션 조회 → Server가 1초마다 조회해 캐싱
- 다중 Client 부하 개선: 최대 다수 Client가 각각 조회 실행 → 캐시된 Status 응답 공유

### 중개 PC

- VPN 기능 검토 후 제거: 특정 SonicWall VPN 기능을 Client에 포함 → 범용성을 위해 제외
- 중개 PC 기능 추가: 직접 연결만 지원 → 중간 PC를 통한 Status 및 RDP 연결 지원
- 중개 RDP Port 범위 추가: `13389~13489`
- 중개 구조 변경: 독립 Bridge에 Server 기능 포함 → Server에 Bridge 기능 통합
- Bridge 설정 관리 추가: 허용 대상 목록, Status Port, RDP Port 저장

### Windows Service

- Server 자동 실행 방식 변경: 사용자 로그인 후 시작 프로그램 실행 → Windows Service 자동 시작
- 다중 사용자 PC 대응: 특정 사용자가 로그인해야 동작 → 로그인 전에도 Server API 동작
- Server UI 유지: Service만 제공 → 사용자가 상태와 로그를 확인할 수 있는 트레이 UI 병행

### 설치 프로그램

- 통합 설치 프로그램 추가: Client/Server 실행 파일 개별 배포 → Inno Setup 하나로 선택 설치
- 구성요소 선택 추가: Client, Server 중복 선택 설치 가능
- 설치 경로 지정 추가: Client와 Server 경로 개별 지정
- 기본 설치 경로 지정:
  - `C:\RDP\Client`
  - `C:\RDP\Server`
- 추가 작업 선택 추가: 바탕화면 아이콘, 시작 메뉴, Server 자동 실행
- 실행 프로세스 검사 추가: 실행 중 덮어쓰기 → 종료 안내와 다시 시도 제공
- 설치 버전 표시 추가: `v1.0.0`
- 삭제 진행률 표시 추가: 삭제 중 화면 멈춤처럼 보임 → Progress Bar 표시

### 로그

- Client Logs 제거: Client에도 빈 Logs 폴더 생성 → Client는 Logs 폴더를 만들지 않음
- Server 로그 위치 변경: 고정된 `C:\RDP\Logs` → Server 설치 경로 아래 `Logs`
- 로그 파일명 변경: 날짜 하이픈 포함 → `Server_yyyyMMdd.txt`
- 반복 로그 축소: 모든 Status 요청 기록 → 정상 조회 로그 제거
- 오류 로그 축소: 전체 Stack Trace 기본 기록 → 핵심 오류 메시지 중심 기록

---

## 2026-05-19 ~ 2026-05-20

### 프로젝트 생성

- C# .NET 8 WinForms 기반 솔루션 생성
- Client와 Server 프로젝트 분리
- Client에서 Server로 HTTP Polling하는 기본 구조 구현
- Windows 원격 데스크톱 `mstsc` 실행 기능 구현

### Server

- RDP 접속 감지 추가
- 접속 로그 항목 추가: 접속 PC IP, 접속 이름, 접속 시작 시간
- 접속 종료 기록 추가: 기존 접속 행에 접속 종료 시간 갱신
- Session ID 표시 변경: UI 표시 → 로그 파일에만 기록
- Server 로그 파일 저장 추가
- Server 상태 1초 갱신 추가
- Server 그리드 오른쪽 빈 공간 제거
- 접속자 이름 표시 변경: 원격 PC 로그인 계정 → 접속한 PC 이름

### Client

- 내 PC 정보 표시 추가: PC 이름, 사용자 계정명, IP 주소
- 사용자 계정명 제거: 실제 기능에서 사용하지 않음
- 원격 PC 추가·수정·삭제 기능 구현
- 등록 정보 추가: PC 이름, IP, ID, PW
- 연결 상태 확인 추가
- 접속자 상태와 접속자 이름 표시 추가
- 상태 확인 방식 변경: 프로그램 실행 후 계속 자동 확인 → PC별 시작/종료 버튼
- 상태 확인 주기 설정: 1초
- 상태 확인 전 원격 접속 버튼 비활성화
- 확인 불가 상태 접속 경고 추가
- mstsc 실행 시 등록한 원격 PC 정보를 사용하도록 연결

### 초기 UI 수정

- Client와 Server 상단 박스 및 목록 테두리 추가
- 상단 박스와 원격 PC 목록의 좌측 정렬 통일
- 하단 HTTP Server 상태 영역 제거
- Listening 정보 영역 제거
- 버튼 텍스트 잘림과 하단 테두리 잘림 수정
- 그리드 행·열 크기 임의 변경 제한
- 프로그램 시작 시 입력창에 자동 포커스되는 문제 수정
- 상태 갱신마다 선택 셀이 이동하는 문제 수정
